using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProtocolHandler : MonoBehaviour {

	private void LoadByUri(Uri uri)
    {
        Debug.Log("I got the message!");
        Debug.Log(uri);

        String access_token = "";

        //if (uri.Fragment != null)
        //{
        //    access_token = uri.Fragment.

        //}
    }
}
